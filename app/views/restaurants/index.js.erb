function replaceRestaurants (innerHTML) {
  const restaurants = document.getElementById('restaurants');
  restaurants.innerHTML = innerHTML;
}replaceRestaurants("<%= j render 'restaurant', restaurants: @restaurants %>");
